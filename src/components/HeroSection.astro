---
import { Image } from "astro:assets";
import bgHero from "../assets/bgHero.jpg";
---

<section
  id="hero"
  class="relative flex flex-col md:flex-row items-center justify-center bg-[url(/assets/bgHero.jpg)] bg-center bg-cover md:bg-backgrounDark md:bg-none min-h-screen px-6 md:px-12"
>
  {/* Overlay solo en mobile */}
  <div class="absolute inset-0 bg-black/50 md:hidden"></div>

  <div class="relative flex flex-col items-center justify-center z-10">
    <h1
      class="text-white text-6xl font-bold text-center md:text-left mb-6 w-full"
    >
      <span class="text-primary">Software </span> a la medida
    </h1>
    <p class="text-white text-lg text-center md:text-left mb-6 md:mb-12">
      Somos un equipo de desarrollo de software especializado en la creación de
      soluciones a la medida. Nos apasiona transformar ideas en productos
      digitales funcionales, atractivos y eficientes.
    </p>
    <a href="#contacto" class="inline-block">
      <button
        class="text-white text-xl md:text-2xl border-2 border-primary rounded-xl py-2 px-4
         bg-transparent md:bg-primary transition-colors duration-300
         hover:bg-primary hover:text-white active:outline-none active:ring-2 active:ring-active active:border-active"
      >
        Contáctanos
      </button>
    </a>
  </div>

  <Image
    src={bgHero}
    alt="bgHero"
    class="hidden md:block md:w-1/2 object-cover"
    loading="eager"
  />
</section>
